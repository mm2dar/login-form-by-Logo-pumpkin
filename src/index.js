import React from 'react';
import ReactDOM from 'react-dom/client';
import LOG from "./app/loginApp";
import "./index.css";


const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
      <LOG></LOG>
  </React.StrictMode>
);
